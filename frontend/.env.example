# API Base URL for backend
# API_BASE_URL=http://localhost:8000

# DEV ONLY: permite previsualizar rutas protegidas (/dashboard, /routines, etc.) sin login
# Solo funciona con NODE_ENV !== "production" (dev/test). No usar en producción.
# DEV_BYPASS_AUTH=true
